if(AISquad_GetState() == AISquadState_Idle && AISquad_GetEvent() == AIEVENT_ENEMYDETECTION && AISquad_732.isDead)
{
	AISquad_ManStationaryGun();
	AISquad_DefaultHandler();
}
else
{
	AISquad_DefaultHandler();
}
