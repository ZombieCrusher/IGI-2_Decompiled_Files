if(AIFunction_GetCurrentEventType() == AIEVENT_CREATE)
{
	AIFunction_DefaultHandler();
}
else
{
	if(AreaActivate_1705.nActive == TRUE && AIFunction_GetCurrentEventType() == AIEVENT_IDLE)
	{
		AIAction_WalkToNode(430, 1);
	}
	else
	{
		AIFunction_DefaultHandler();
	}

}
