if(AIFunction_GetCurrentEventType() == AIEVENT_CREATE)
{
	AIFunction_DefaultHandler();
}
if(AIFunction_GetCurrentEventType() == AIEVENT_IDLE && SCamera_112.isDetection)
{
	AIAction_LookAtNode(2);
}
else
{
	AIFunction_DefaultHandler();
}
