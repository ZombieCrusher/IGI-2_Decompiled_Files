if(AIFunction_GetCurrentEventType() == AIEVENT_CREATE)
{
	AIFunction_DefaultHandler();
}
else
{
	if(AIFunction_GetCurrentEventType() == AIEVENT_IDLE)
	{
		AIAction_WalkToNode(361, 1);
		AIAction_LookAtNode(179, 1);
	}
	else
	{
		AIFunction_DefaultHandler();
	}

}
